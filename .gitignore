node_modules
dist
.DS_Store
*.log

# Environment — .env alone does NOT match .env.local, so match the family.
.env
.env.*
!.env.example

# Firebase CLI local state and emulator exports
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
