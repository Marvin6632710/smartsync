{
  "name": "smartsync-functions",
  "private": true,
  "description": "The trusted sender: turns an inbox record into a browser push.",
  "type": "module",
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "node scripts/sync-locales.mjs",
    "serve": "npm run build && firebase emulators:start --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^6.4.0",
    "i18next": "^25.5.2"
  }
}
